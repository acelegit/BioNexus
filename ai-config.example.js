/* Copiază acest fișier în `ai-config.js` și pune cheia ta reală.
   ai-config.js este gitignorat (nu se comite — conține secretul).
   Atenție: pe un site static cheia e vizibilă în browser. */
window.BIONEXUS_AI = {
  baseUrl: "https://api.voidai.app/v1",
  apiKey: "PUNE_CHEIA_TA_AICI",
  model: "gpt-5.4-mini",
};


//salut